/**
 * @constant
 * @name handledFormInputTypes
 * @description The various form input types that can be handled by the library.
 *
 * @author Timothée Simon-Franza
 */
const HANDLED_FORM_INPUT_TYPES = [
	'input',
	'select',
	'textarea',
];

export {
	// eslint-disable-next-line import/prefer-default-export
	HANDLED_FORM_INPUT_TYPES,
};
